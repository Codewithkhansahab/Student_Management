<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Student Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background: #f3f4f6;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    .card-profile {
      width: 420px;
      border: none;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 6px 18px rgba(0,0,0,0.08);
      background-color: #fff;
      transition: all 0.3s ease-in-out;
    }

    .card-profile:hover {
      transform: translateY(-6px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .card-header-custom {
      background: linear-gradient(135deg, #6EE7B7, #3B82F6);
      color: white;
      padding: 15px 20px;
      text-align: center;
    }

    .card-header-custom h4 {
      margin: 0;
      font-size: 20px;
    }

    .card-body-custom {
      padding: 20px;
    }

    .info-row {
      display: flex;
      align-items: center;
      margin-bottom: 12px;
      color: #374151;
      font-size: 15px;
    }

    .info-row i {
      width: 22px;
      color: #3B82F6;
      margin-right: 10px;
    }

    .btn-back {
      display: block;
      margin: 0 auto 20px;
      background: #3B82F6;
      color: white;
      padding: 5px 18px;
      border-radius: 20px;
      border: none;
      font-size: 14px;
      text-decoration: none;
    }

    .btn-back:hover {
      background: #2563EB;
    }
      @media screen and (max-width: 768px) {
  .custom-box {
    flex-direction: column;
    text-align: center;
  }
}

  </style>
</head>
<body>

  <div class="card-profile">
    <div class="card-header-custom">
      <h4><i class="fas fa-user-circle"></i> <%= student.name %></h4>
    </div>
    <div class="card-body-custom">
      <div class="info-row"><i class="fas fa-envelope"></i> <%= student.email %></div>
      <div class="info-row"><i class="fas fa-book"></i> <%= student.course %></div>
      <div class="info-row"><i class="fas fa-user-clock"></i> <%= student.age %> years</div>
      <div class="info-row"><i class="fas fa-venus-mars"></i> <%= student.gender %></div>
      <div class="info-row"><i class="fas fa-birthday-cake"></i> <%= student.dob %></div>
      <div class="info-row"><i class="fas fa-phone"></i> <%= student.phone %></div>
      <div class="info-row"><i class="fas fa-map-marker-alt"></i> <%= student.address %></div>
      <div class="info-row"><i class="fas fa-calendar-day"></i> <%= student.admission_date %></div>
      <div class="info-row"><i class="fas fa-check-circle"></i> <%= student.status %></div>
    </div>
    <a href="/givelist" class="btn-back">⬅ Back to List</a>
  </div>

</body>
</html>
